syntax = "proto3";

package rk;

option go_package = "./pb";

message KeySignal {
  string os = 1;
  int64 code = 2;
  bool keyDown = 3;
}

message Void{}

service RemoteKey {
  rpc KeySignalStream(stream KeySignal) returns(Void) {}
}

