syntax = "proto3";

package rk;

option go_package = "./pb";

message KeySignal {
  int64 code = 1;
  int64 event = 2;
  string os = 3;
}

message Void{}

service RemoteKey {
  rpc KeySignalStream(stream KeySignal) returns(Void) {}
}

